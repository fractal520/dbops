alarm_message = {
    'connect_num': "the database ${dbname} processes is used ${percent}%, used processes ${alarm_num},max processes ${max_num}",
    'connectivity': "the database ${dbname} can't connected",
    'fra usage': "the database ${dbname} fra is used ${percent}%."
}
